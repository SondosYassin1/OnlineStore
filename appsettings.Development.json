{
  //This connection string tells EF Core how to connect to the SQL Server instance 
  //(e.g., server name, database name, authentication method). This connection string tells EF Core how to connect to the SQL Server instance (e.g., server name, database name,authentication method).
  //"ConnectionStrings": {
  //  //We put the connections role in the conntectionsettings
  //  "DefaultConnection": "Server=.;Database=OnlineStore;Trusted_Connection=true;MultipleActiveResultSets=true;TrustServerCertificate=true;"
  //}, // Th Login method is Trusted_Connection=true;
  // MultipleActiveResultSets=true; ---> this is always true
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
